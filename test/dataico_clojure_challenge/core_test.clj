(ns dataico-clojure-challenge.core-test
  (:require [clojure.test :refer :all]
            [dataico-clojure-challenge.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
